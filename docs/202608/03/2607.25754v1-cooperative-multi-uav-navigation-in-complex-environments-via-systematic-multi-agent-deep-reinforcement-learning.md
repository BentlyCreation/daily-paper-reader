---
title: Cooperative Multi-UAV Navigation in Complex Environments via Systematic Multi-Agent Deep Reinforcement Learning
title_zh: 复杂环境下基于系统性多智能体深度强化学习的协同多无人机导航
authors: "Yu Su, Nabil Aouf"
date: 2026-07-28
pdf: "https://arxiv.org/pdf/2607.25754v1"
tags: ["query:lbm"]
score: 9.0
evidence: 结合行为克隆示范的多智能体深度强化学习用于多无人机协作导航
tldr: 多无人机协同导航面临局部最优陷阱、稀疏奖励、智能体学习失衡与跨场景泛化不足等挑战。本文提出多智能体深度强化学习框架，通过记忆与新颖性估计的协调探索、分层示范缓冲与差异监督、安全双条件课程调度、以及基于局部几何特征的结构感知混合专家，系统解决上述问题。在混合静动态障碍环境中，该方法实现了高协作成功率、导航鲁棒性、零样本跨场景泛化与动态干扰适应。该框架为复杂环境下多无人机协同导航提供了系统性技术路径，具有重要的工程应用价值。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.25754v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 844, \"height\": 1441, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25754v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 916, \"height\": 536, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25754v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 469, \"height\": 362, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25754v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 920, \"height\": 370, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25754v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 911, \"height\": 525, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25754v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 913, \"height\": 531, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25754v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 919, \"height\": 476, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.25754v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 927, \"height\": 698, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.25754v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 755, \"height\": 1185, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.25754v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 768, \"height\": 421, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.25754v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 785, \"height\": 347, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.25754v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 625, \"height\": 178, \"label\": \"Table\"}]"
motivation: 针对多无人机协同导航中局部最优陷阱、稀疏奖励、智能体学习失衡及跨场景泛化不足等关键挑战，构建系统化强化学习解决方案。
method: 提出多智能体DRL框架，含记忆新颖性协调探索、分层示范克隆、安全双条件课程调度及结构感知混合专家门控，实现结构感知泛化。
result: 在混合静动态障碍模拟中，协作成功率、导航鲁棒性、零样本跨场景迁移和动态适应性均表现优异，验证框架有效性。
conclusion: 该工作系统解决了多无人机协同导航的多个核心难题，为复杂动态环境下可靠、可泛化的协同策略提供了新范式。
---

## 摘要
多智能体无人机在复杂环境中的协同导航面临关键挑战，包括局部最优陷阱、稀疏奖励、智能体间学习失衡以及跨场景泛化不足。本文提出了一种多智能体深度强化学习框架，通过协调探索、演示利用、安全课程调度和结构感知泛化来解决这些问题。首先，一种结合已访问状态记忆、方向性新颖性估计和惩罚反向传播的感知机制使智能体能够主动检测并逃离局部最优。其次，一个具有分层行为克隆的分层协作演示缓冲区按团队协作程度管理轨迹，并对演员网络施加差异化监督，提高了在稀疏协作信号下的演示利用率。第三，一种安全感知的双条件课程调度机制通过训练期间的回测和经验预填充来复习已掌握场景，在确保任务性能和飞行安全的同时抑制灾难性遗忘。对于泛化，从传感器读数计算出的局部几何特征被抽象为域参数，通过该参数，结构感知门控网络和专家混合机制根据局部结构模式而非特定场景坐标来调节策略，从而无需接触目标环境即可实现跨场景迁移。该框架进一步在静态-动态混合障碍设置下进行了验证，显示出对动态干扰的鲁棒适应性。仿真结果证实了其在协作成功率、导航鲁棒性、零样本跨场景泛化和动态环境适应性方面的强大性能。

## Abstract
Cooperative navigation of multi-agent UAVs in complex environments faces key challenges including local optima traps, sparse rewards, learning imbalance among agents, and insufficient cross-scenario generalisation. This paper proposes a multi-agent deep reinforcement learning framework that addresses these issues through coordinated exploration, demonstration exploitation, safe curriculum scheduling, and structure-aware generalisation. First, a perception mechanism combining memory of visited states, directional novelty estimates, and penalty backpropagation enables agents to proactively detect and escape local optima. Second, a hierarchical collaborative demonstration buffer with tiered behaviour cloning manages trajectories by degree of team collaboration and applies differential supervision to the actor network, improving demonstration utilisation under sparse collaborative signals. Third, a safety-aware dual-condition curriculum scheduling mechanism reviews mastered scenarios through back-testing and experience pre-filling during training, suppressing catastrophic forgetting while ensuring both task performance and flight safety. For generalisation, local geometric features computed from sensor readings are abstracted into a domain parameter, through which a structure-aware gating network and mixture-of-experts mechanism condition the policy on local structural patterns rather than scenario-specific coordinates, enabling cross-scenario transfer without exposure to the target environment. The framework is further validated under mixed static-dynamic obstacle settings, showing robust adaptability to dynamic disturbances. Simulation results confirm strong performance in collaboration success rate, navigation robustness, zero-shot cross-scenario generalisation, and dynamic environment adaptability.

---

## 论文详细总结（自动生成）

# 论文总结：《复杂环境下基于系统性多智能体深度强化学习的协同多无人机导航》

## 1. 核心问题与整体含义（研究动机与背景）

该论文聚焦于**多无人机（Multi-UAV）在局部可观测、结构复杂环境中的协同导航问题**，其核心动机可归纳为以下四点相互交织的挑战：

- **局部最优陷阱**：在迷宫等具有复杂空间结构的受限环境中，无人机容易陷入结构性死胡同或局部停滞，传统"好奇心驱动"探索方法缺乏与任务目标耦合的逃脱机制。
- **稀疏协作奖励**：全部智能体同时到达目标的事件在训练早期极为稀少，导致协作成功信号稀薄，示范数据利用效率低下。
- **智能体间学习失衡**：不同智能体在共享环境中学习进度不一致，现有方法缺少针对个体滞后问题的数据管理与策略优化手段。
- **跨场景泛化不足**：现有策略倾向于记忆训练场景的坐标信息，无法利用迷宫环境中的**局部几何结构重复性**实现零样本迁移。

论文的核心价值在于提出一个**系统性框架**（而非单一算法模块），将上述挑战视为相互耦合的复合问题，通过四项协同机制联合解决，为复杂动态环境下的多无人机自主协同导航提供了统一的技术路径。

## 2. 论文提出的方法论

### 2.1 整体框架

- 基于**多智能体软演员-评论家（MASAC）**算法，采用**集中式训练-分布式执行（CTDE）**范式。
- 每个无人机维护独立的**混合专家演员网络（MoE Actor）**，执行阶段仅依赖本地观测历史（长度 𝑘=16 的滑动窗口），无显式通信；集中式评论家负责全局价值估计。
- 局部观测向量维度为 33（目标向量、速度、8 扇区 LiDAR 读数、帧间差、前序动作、访问记忆特征、结构域参数 ω）。

### 2.2 四大核心机制

**（1）局部最优诊断与执行级干预机制**

- 三子模块：**访问状态记忆**（网格计数表）、**局部最优诊断**（四条件判定：悬停、重复、阻塞、游荡）、**执行级干预**（方向新颖性评分函数）。
- 关键设计：该机制**不引入任何可训练参数**，直接在动作执行层覆盖策略输出，主动引导智能体逃离停滞区域。
- 方向评分函数：𝑓(ϕₖ)=𝑤_𝜈·𝜈(ϕₖ)+𝑤_𝑐𝑙·𝑐𝑙(ϕₖ)−𝑤_𝑟·𝑝𝑒𝑛(ϕₖ)，融合方向新颖性、前向净空与回溯惩罚；检测到死胡同时切换为净空主导模式。
- 引入 **LiDAR 派生通道约束指标 κₜ**，在目标被遮挡时动态补偿距离奖励，消除走廊强制绕行期间的"奖励真空"。

**（2）层次化示范缓冲与分级行为克隆**

- 双缓冲架构：**协作成功缓冲器 D_coop**（全智能体到达）+ **个体成功缓冲器 D_i**（至少一个智能体到达），按场景级别和起终点对级别实行配额管理。
- 行为克隆损失直接施于演员网络，采用**差异化权重**：𝐿_BC=𝔼_{D_coop}[‖π(𝑜)−𝑎*‖²]+𝜆𝔼_{D_i}[‖π(𝑜)−𝑎*‖²]，𝜆<1。
- 克隆系数 β_coop 随 D_coop 占用率从 β_min 线性增至 β_max，避免早期过度约束探索，同时**消除两阶段策略蒸馏的累积近似误差**。

**（3）安全感知双条件课程调度**

- 课程升级需**同时满足**协作成功率阈值 η≥τ_η 与碰撞率上限 ξ≤τ_ξ，防止策略利用高风险行为换取表面成功。
- 每次阶段升级后执行**回溯性全量评估**与**经验预填充**（每场景 15 条新轨迹注入回放缓冲），配合历史场景混合采样比例（p_hist=0.40）抑制灾难性遗忘。
- 团队奖励引入同步感知机制：仅当所有智能体同时位于目标邻域时激活协作奖励，并按智能体间到达时间差施加惩罚。

**（4）结构感知泛化机制**

- 将迷宫环境的局部几何结构抽象为**域参数 ω∈[0,1]⁴**，由 8 扇区 LiDAR 读数确定性计算，无需人工标注或目标场景先验知识；四个分量分别表征**死胡同、单侧墙、狭窄通道、开放区域**。
- 演员网络采用 **MoE 架构**：共享 LSTM 编码器提取时序特征 ℎ，结构感知门控网络以 ω 为输入生成专家混合权重 𝑔=softmax(MLP_gate(ω))，加权融合多个专家头的动作均值与对数标准差。

### 2.3 训练流程

完整训练循环整合于 Algorithm 1：预热期随机动作初始化缓冲 → 每 50 步联合采样三类缓冲更新策略 → 每 100 episodes 评估双条件 → 达标后触发回溯评估与经验预填充 → 推进下一课程阶段。

## 3. 实验设计

### 3.1 模拟平台与场景

- **模拟平台**：Microsoft AirSim + Unreal Engine 4，3D 渲染与物理引擎。
- **传感器配置**：单通道水平全向 LiDAR（12 m 量程，10 r/s，5000 点/s），聚合为 8 等角扇区取最小量程。
- **场景设置**：7 个训练场景 + 1 个未见测试场景（maze mix）：
  - maze 01–03：基础静态障碍场景；
  - maze 04–06：含明显局部最优特征的复杂障碍布局；
  - maze 07：含 3 个横向往复运动动态障碍的更大规模场景（60m×40m）；
  - maze mix：训练中完全未见、含动态障碍的混合测试场景。
- 每场景配置多个起点-终点对，包含正向、反向及智能体互换路径，防止过拟合特定空间配置。

### 3.2 评价指标

协作成功率 η、碰撞率 ξ、平均终端距离、智能体间到达时间差四项指标，均在 20 episodes 上评估。

### 3.3 对比方法

- **MAPPO**（Yu et al., 2022）：多智能体 PPO 基线；
- **标准 MASAC**：去除本文全部机制的基础版本；
- **消融变体**：w/o intervention、w/o demo、w/o MoE 三种。

### 3.4 实验内容

| 实验类型 | 场景与条件 | 规模 |
|---|---|---|
| 总体性能评估 | 全部 7 训练场景 + maze mix | 每场景 20 episodes |
| 基线对比 | maze 05 + maze mix | 3 方法 × 2 场景 × 20 episodes |
| 消融实验 | maze 05 | 4 变体 × 1500 训练 episodes |
| 泛化对比 | maze mix | 2 变体 × 20 episodes |

## 4. 资源与算力

- **GPU**：单块 NVIDIA RTX 6000 Ada Generation（49 GB VRAM），CUDA 12.8。
- **训练时长**：论文**未明确报告**训练所需的具体时长或 GPU 数量（可推断为单卡）。
- **环境交互量**：每次训练约 1500 episodes，但未说明每个 episode 的步数上限，因此无法换算总环境步数。

需要指出：论文对于算力投入的透明度有限，缺少训练时间与吞吐量的量化数据，这在一定程度上影响读者对方法工程成本的评估。

## 5. 实验数量与充分性

### 5.1 实验覆盖度

- **总体评估**覆盖 8 个场景，包含静态、局部最优密集、动态障碍三种类型，场景多样性较好；
- **基线对比**在训练场景（maze 05）与未见场景（maze mix）上各 20 episodes，足够展示零样本迁移的差异；
- **消融实验**系统检验了干预机制、示范缓冲、MoE 三项设计的独立贡献，归因较为清晰；
- **泛化实验**对比了全框架与 w/o MoE 变体，并给出了门控权重分布作为机制层面的行为证据。

### 5.2 充分性与公平性

- 所有方法在相同环境与碰撞检测条件下训练 1500 episodes，基础配置公平。
- 需注意：MAPPO 使用 21 维观测（不含域参数 ω），全框架使用 33 维观测。论文视为"框架固有设计差异"，结论可接受，但此差异使其与基线的性能对比并非完全"同条件下"的比较。
- 每场景仅 20 个评估 episodes，统计置信度偏低；尤其是碰撞率指标，0.050 的差距在如此小样本下可能不显著，未报告方差或置信区间。
- 课程阈值（τ_η=0.75, τ_ξ=0.40）与迷宫 04 的实测值（η=0.800, ξ=0.400）恰好持平，碰撞率等于上限，反映出该场景的安全性保障处于临界状态。

总体而言，实验设计逻辑清晰、消融完整、机制证据充分，但在**评估规模统计严谨性**方面有提升空间。

## 6. 论文的主要结论与发现

- **系统性解决多维挑战**：提出的框架在全部静态迷宫训练场景上通过课程阈值，验证了局部最优逃离、示范利用与安全调度机制的综合有效性。
- **零样本跨场景泛化成功**：在未见测试场景 maze mix 上实现 η=0.750、ξ=0.100，训练-测试成功率差距仅 0.050（vs MAPPO 的 0.250），证实结构感知 MoE 机制可有效泛化。
- **MoE 门控是泛化的关键**：全框架在 maze mix 上 η=0.750 vs w/o MoE 的 0.250；门控权重分析显示两个 UAV 在同一场景中对不同专家产生自动分化的激活模式，证明 gating 网络学习的是局部结构条件映射而非全局空间记忆。
- **干预机制决定训练效率**：去除干预机制后成功率前 800 episodes 接近零、碰撞率飙至 0.9，说明执行级逃脱引导对早期探索至关重要。
- **示范缓冲显著提升样本效率**：去除后策略变保守但协作信号极弱，成功率长期停滞，行为克隆有效缓解了稀疏奖励问题。

## 7. 优点

- **问题定位精准，方案系统性强**：四个模块分别对应四个核心痛点，且模块间存在良好的互补耦合（如干预机制帮助产生高质量示范数据，示范数据反哺策略收敛），而非孤立修补。
- **干预机制的工程价值高**：零附加参数的执行级局部最优干预，可在不改变模型规模的前提下任意扩展智能体数量，具备实际部署潜力。
- **示范利用设计精细**：按协作完成度分层存储、场景级/起终点对级配额管理、差异化克隆权重、动态调整的克隆系数，对示范数据的处理粒度远超一般 LfD 方法。
- **安全与性能联合保障**：双条件课程调度首次将碰撞率纳入阶段推进硬约束，配合回测与预填充有效抑制灾难性遗忘。
- **泛化机制有理论依据**：将环境几何重复性编码为可解释的四维域参数，门控权重分布作为机制级证据使"结构感知而非坐标记忆"的主张有说服力。
- **写作规范、可复现性强**：超参数全表公开、训练算法伪代码完整、观测向量构成表清晰。

## 8. 不足与局限

- **仅验证两架无人机配置**：论文结论建立在 N=2 的规模上，虽然机制声称"可扩展至任意数量智能体"，但缺少三架及以上的实验证据，其扩展性主张未被直接验证。
- **仿真与实机差距未弥合**：实验完全在 AirSim/UE4 中完成，未涉及真实飞行平台验证，传感器噪声、延迟、执行器动力学差异均可能影响方法在室外的实际表现。
- **动态障碍形式单一**：仅包含横向往复运动的障碍物，未覆盖随机移动障碍、多向运动或多动态体交互等更复杂场景。
- **局部最优类型的普遍性有限**：干预机制的触发条件（悬停/重复/阻塞/游荡）针对迷宫结构设计，在更开放的室外环境中的适用性值得商榷。
- **评估规模偏小**：每场景仅 20 episodes，未报告方差/置信区间；在如此小样本上 0.04 的差值可能不具备统计显著性。
- **消融实验仅覆盖单一场景（maze 05）**：局部最优干预效果在最难的 maze 04 上未做消融；MoE 泛化优势也仅在单一未测试场景上展示，若增加多个未见场景会更具说服力。
- **高难度场景的碰撞率偏高**：maze 04 碰撞率 0.400 恰好达到阈值上限，说明"安全保证"在此类复杂场景下仍有较大提升空间。
- **跨场景机制依赖特征工程**：域参数 ω 的分量选取依赖人工对场景结构类型的先验判断，不同环境类型（如森林、城市街区）需要重新设计特征，通用性受限。
- **智能体规模与任务复杂度尚未扩展**：任务目标仅为到达指定位置，未涉及编队保持、区域覆盖等更高层级的协同任务。

（完）
