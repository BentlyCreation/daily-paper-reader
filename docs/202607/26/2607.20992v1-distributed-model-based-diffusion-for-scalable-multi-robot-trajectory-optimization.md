---
title: Distributed Model-Based Diffusion For Scalable Multi-Robot Trajectory Optimization
title_zh: 面向可扩展多机器人轨迹优化的分布式基于模型的扩散
authors: "Haejoon Lee, Xinyi Wang, Taekyung Kim, Dimitra Panagou"
date: 2026-07-23
pdf: "https://arxiv.org/pdf/2607.20992v1"
tags: ["query:lbm"]
score: 9.0
evidence: 分布式多机器人轨迹优化，基于模型的扩散方法
tldr: 多机器人轨迹优化在复杂环境中面临高维推理和全局信息需求挑战。现有集中式模型扩散扩展到多机器人时存在维数灾难和全局访问限制。本文提出分布式模型扩散DMBD，将反向扩散分解为各机器人独立去噪的条件扩散，服务器聚合广播轨迹估计。在多个协调场景中，DMBD实现亚秒级求解且显著优于基线方法，展现了强可扩展性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.20992v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1753, \"height\": 946, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.20992v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 900, \"height\": 575, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.20992v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 911, \"height\": 1095, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.20992v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1756, \"height\": 765, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.20992v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 683, \"height\": 191, \"label\": \"Table\"}]"
motivation: 集中式模型扩散扩展到多机器人系统时，维数灾难导致样本效率低，且需全局访问所有机器人的动力学和约束。
method: 提出分布式服务器-机器人框架，将反向扩散分解为各机器人独立去噪的条件扩散，服务器聚合并广播其他机器人轨迹估计。
result: 在目标交换、多层覆盖、停车和高峰场景中，DMBD在亚秒内解决协调任务，性能显著优于现有基线。
conclusion: DMBD实现了多机器人轨迹优化的强可扩展性，高效解决了高度非凸环境中的复杂协调问题。
---

## 摘要
多机器人系统的轨迹优化仍然是一个关键挑战，尤其是在高度非凸、非线性和不可微的环境中导航时。尽管基于模型的扩散（MBD）最近作为一种有前景的基于采样的优化范式出现在单机器人轨迹生成中，但将其扩展到多机器人系统会导致一个集中式的高维推理问题，该问题（i）由于维数灾难而样本效率低下，（ii）需要全局访问所有机器人的动力学、约束和目标。为了解决这个问题，我们提出了分布式基于模型的扩散（DMBD），一种分布式的服务器-机器人框架，它将逆向扩散过程分解为局部条件逆向扩散过程。这种分解使得每个机器人能够在其自身的控制子空间内独立迭代执行去噪，同时以由服务器聚合和广播的其他机器人的当前轨迹估计为条件。在目标交换、多楼层覆盖、停车和高峰时段场景中的大量仿真表明，DMBD实现了强大的可扩展性，在亚秒级内解决了许多具有挑战性的协调任务，并显著优于现有基线。

## Abstract
Trajectory optimization for multi-robot systems remains a critical challenge, particularly when navigating highly non-convex, non-linear, and non-differentiable environments. While Model-Based Diffusion (MBD) has recently emerged as a promising sampling-based optimization paradigm for single-robot trajectory generation, extending it to multi-robot systems results in a centralized, high-dimensional inference problem that (i) suffers from poor sample efficiency due to the curse of dimensionality and (ii) requires global access to all robots' dynamics, constraints, and objectives. To address this, we propose Distributed Model-Based Diffusion (DMBD), a distributed server-robot framework that decomposes the reverse diffusion process into local conditional reverse diffusion processes. This decomposition enables each robot to iteratively perform denoising independently within its own control subspace while conditioning on the current trajectory estimates of the other robots that are aggregated and broadcast by the server. Extensive simulations in goal swapping, multi-floor coverage, parking, and rush-hour scenarios demonstrate that DMBD achieves strong scalability, solving many challenging coordination tasks in sub-seconds and significantly outperforming existing baselines.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：多机器人系统在共享工作空间中进行无碰撞轨迹优化是一个关键挑战，尤其是在高度非凸、非线性、不可微的环境中。传统的梯度方法容易陷入局部极小值，计算成本高；基于采样的优化（SBO）方法如CEM、MPPI等虽有优势，但扩展到多机器人时面临维数灾难和全局信息需求。
- **背景问题**：最新提出的Model-Based Diffusion（MBD）作为一种单机器人SBO方法，通过迭代去噪在多模态轨迹分布上优化，无需离线学习。但将其直接扩展到多机器人系统会导致集中式高维推理问题：(i) 样本效率因维数灾难而急剧下降；(ii) 需要全局访问所有机器人的动力学、约束和目标，在异构团队中不切实际。
- **核心挑战**：如何在保持MBD高效性的同时，实现可扩展、分布式的多机器人轨迹优化，避免共享敏感局部信息。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：提出Distributed Model-Based Diffusion (DMBD)，一种服务器-机器人分布式框架，将集中式的逆向扩散过程分解为**局部条件逆向扩散过程**。每个机器人独立在其控制子空间内进行去噪，同时以服务器聚合广播的其他机器人当前轨迹估计为条件。
- **关键技术细节**：
  - **问题建模**：每个机器人k有局部目标函数J_k、动力学f_k、涉及自身与其他机器人的耦合约束g_{k,p}。全局问题被转化为概率推理形式：目标分布p_0(Y) ∝ exp(-ϕ(Y)/λ)，其中ϕ包含所有成本与惩罚项。
  - **前向（加噪）过程**：与标准MBD一致，逐步将干净轨迹噪声化为标准高斯分布，共M步。
  - **逆向（去噪）过程**（DMBD核心）：
    - 初始化：每个机器人采样Y_k^{(M)} ~ N(0, I)，发送状态轨迹给服务器，服务器聚合广播τ^{(M)}。
    - 对每步i=M,...,1：每个机器人k接收其他机器人轨迹τ_{-k}^{(i)}，采样S_k个候选控制轨迹，根据局部条件目标分布p_k^0(·|τ_{-k})计算加权平均Y_k^(i)，估计局部条件得分∇log p_k^i，然后按公式(16)更新Y_k^{(i-1)}。随后每个机器人通过动力学滚动得到新状态轨迹，发送给服务器。
    - 服务器聚合后广播，迭代至i=1。
  - **理论分析**：命题1给出了集中式得分与局部条件得分之间的误差上界：该界依赖于H_k Lipschitz常数和其他机器人最优轨迹与当前估计的偏差。当约束完全解耦时，H_k=0，与独立运行MBD等价。
  - **算法流程**见Algorithm 1。

## 3. 实验设计：数据集/场景、benchmark、对比方法

- **实验场景**：
  1. **目标交换（Goal-Swap）**：圆形机器人（半径1.5m，双积分器动力学）和矩形机器人（3.0×5.0，自行车模型）各N个，初始位置在圆周上，目标在对面。N从2到20，规划步长T从200起每增加一个机器人加5步。评估成功率和规划时间。
  2. **多层覆盖（Multi-Floor Coverage）**：异构矩形机器人团队（小型和大型）在两楼层建筑中导航，小型机器人可通过电梯到二楼，大型机器人留在一楼。N从2到10，T从250起每增加一个机器人加25步。随机初始化50次。
  3. **停车（Parking）**：两个矩形机器人，需要协作让其中一个先离开停车位再返回。50个随机初始位置试验。
  4. **高峰时段（Rush Hour）**：四个矩形机器人，仅允许纵向移动，其中机器人1需要离开被其他机器人堵塞的出口。50个随机初始纵向位置试验。
- **benchmark**：对比四种基于采样的方法：
  - Cross-Entropy Method (CEM)
  - Model Predictive Path Integral (MPPI)
  - Model-Based Diffusion (MBD) [集中式]
  - D4orm [集中式，带变形迭代]
- **公平性设计**：所有方法使用相同的规划步长T、去噪步数/迭代次数M、每个机器人采样500个候选轨迹（总样本数一致），成本参数先通过MBD在N=2情况下调优一次。

## 4. 资源与算力

- 论文明确说明：所有仿真使用Python和JAX进行GPU加速，运行计算机配置为：
  - CPU：12th Gen Intel® Core™ i9-12900KF
  - RAM：64 GB
  - GPU：Nvidia RTX 3080 Ti
- **未明确说明**：训练时长（各实验耗时数据以规划时间形式给出，但未汇总总运行时长或GPU使用小时数）。

## 5. 实验数量与充分性

- **实验数量**：
  - 目标交换：每个N值（2,4,6,...,20）进行50次试验（不同随机种子）。
  - 多层覆盖：每个N值（2,4,6,8,10）进行50次试验。
  - 停车和高峰时段：各50次试验。
  - 总计约：目标交换10个N×50=500，多层覆盖5个N×50=250，停车50，高峰50，共850次试验。
- **充分性**：实验覆盖了不同机器人数量（小到大）、不同动力学（双积分、自行车）、不同几何形状（圆形、矩形）、不同任务复杂度（对称交换、非对称覆盖、严格协作、约束谜题）。对比方法全面，指标（成功率和规划时间）合理。每个场景多次随机试验，结果统计可靠。
- **客观公平性**：采样总数一致，参数先调优后固定，未针对DMBD做过拟合调整。但未做消融实验（如不同S_k的影响、不同M的影响）。

## 6. 论文的主要结论与发现

- **可扩展性**：DMBD在成功率和规划时间上均优于所有基线，尤其是在大型团队（N=20）时，基线因OOM失败，DMBD仍能在亚秒内完成规划。
- **协作能力**：在停车和高峰时段场景中，DMBD无需分享全局目标即可实现协调行为（如机器人暂时离开目标让路），成功率达92%和96%。
- **计算效率**：DMBD分解后每个机器人仅在其局部子空间（维度mkT）进行去噪，显著降低了推理复杂度，因此规划时间几乎不随N增长而爆炸。
- **理论支持**：给出了分布式得分与集中式得分误差的解析上界，阐明了DMBD的近似合理性。

## 7. 优点：方法或实验设计上的亮点

- **方法亮点**：
  - **分布式去中心化**：每个机器人仅需知道自身动力学、目标和约束，无需共享给服务器，保护隐私且支持异构团队。
  - **零阶优化**：不要求目标或约束可微，可处理非平滑、非凸问题。
  - **通信高效**：每步只传输一个当前轨迹，而非多个样本，通信开销低。
  - **可扩展性**：从理论上（维数降低）和实验上（成功解决N=20问题）证明了可扩展性。
- **实验亮点**：
  - 覆盖多种真实世界场景（仓储、搜索、停车、高峰）。
  - 对比方法选取全面，包括最新的D4orm。
  - 严格保证公平性（相同超参数、相同总样本数）。

## 8. 不足与局限

- **理论局限**：
  - 命题1的误差界依赖局部去噪器的Lipschitz假设(H_k)，该假设在实际中不易验证。
  - 有限样本分析未给出，实际中S_k为有限，误差可能超出理论界。
- **实验局限**：
  - 未测试真实机器人硬件或更复杂的动力学（如非完整约束的更多变体）。
  - 未做消融实验（如不同条件策略、不同S_k对性能的影响）。
  - 未讨论通信延迟、服务器单点故障等实际部署问题。
  - 只做了离线规划，未明确评估在线实时重规划能力（尽管作者提到“算法可用于在线”但实验仅离线）。
- **应用限制**：
  - 依赖服务器汇总广播，在完全分布式无服务器场景下不适用。
  - 对耦合约束的假设（Assumption 1：约束对称）可能在实际中不满足。
  - 没有考虑感知不确定性或环境动态变化。

（完）
