---
title: "Tensor-Coord: Algebraic Decomposition of Joint Plan Tensors for Conflict-Free Multi-Agent LLM Planning"
title_zh: "Tensor-Coord: 用于无冲突多智能体LLM规划的联合计划张量的代数分解"
authors: Mudit Rastogi
date: 2026-06-15
pdf: "https://arxiv.org/pdf/2606.16478v1"
tags: ["query:lbm"]
score: 7.0
evidence: 通过张量分解实现无冲突多智能体规划，直接解决MAPF中的碰撞避免
tldr: "多智能体LLM规划中独立生成的计划常导致冲突。本文提出Tensor-Coord框架，将联合计划表示为三阶张量，利用CP和Tucker分解识别潜在协调结构，定义可计算的协调复杂度度量，局部化冲突，并生成自然语言约束用于迭代重规划。实验显示2/3/4智能体任务中冲突-free计划收敛率100%/80%/60%。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.16478v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1686, \"height\": 1156, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16478v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1589, \"height\": 1455, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16478v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1565, \"height\": 847, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16478v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1702, \"height\": 554, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.16478v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1755, \"height\": 318, \"label\": \"Table\"}]"
motivation: 多智能体LLM规划因独立生成计划导致空间碰撞、资源竞争和时间死锁等协调失败。
method: 将N智能体联合计划表示为张量，通过CP和Tucker分解提取协调结构，定义协调复杂度度量，并生成约束进行迭代重规划。
result: "2/3/4智能体任务平均1.4/3.2/4.0次迭代收敛到冲突-free计划，成功率100%/80%/60%。"
conclusion: 张量分解有效量化协调复杂度并指导冲突解决，为多智能体LLM规划提供可扩展方法。
---

## 摘要
大型语言模型（LLM）在多智能体规划中仍然存在局限性，因为独立生成的计划可能导致协调失败，例如空间碰撞、资源争用和时间死锁。我们提出Tensor-Coord，一个多重线性代数框架，它将N个智能体的联合计划表示为一个三阶张量\(T \in R^{N \times H \times A}\)，维度分别对应智能体、时间步和动作。使用Canonical Polyadic（CP）分解和Tucker分解来识别潜在的协调结构。最小ε近似CP秩R*定义了一个可计算的协调复杂度度量，\(CC(Pi)=(R*-N)/N\)。我们证明了R*=N是计划独立性的必要充分条件。残差\(E=T-T_{R*}\)定义了智能体对、时间步和动作上的冲突分数，无需领域特定规则即可定位失败。Tucker因子提供了可解释的智能体角色、时间阶段和动作聚类，这些被转化为自然语言约束，用于迭代LLM重规划。在多机器人配送任务上的实验设置了Easy（2个智能体，5x5网格）、Medium（3个智能体，5x5网格）和Hard（4个智能体，5x5网格）场景，结果显示：在2个智能体情况下，平均1.4次迭代内100%收敛到无冲突计划；3个智能体情况下，平均3.2次迭代内80%收敛；4个智能体情况下，平均4.0次迭代内60%收敛。CP秩近似线性增长，\(R*(N) = 3.9N + 0.5\)，支持其作为协调复杂度预测因子的用途。

## 速览
**TLDR**：多智能体LLM规划中独立生成计划易导致空间碰撞、资源竞争等协调失败。本文提出Tensor-Coord框架，将N个智能体的联合计划表示为三阶张量，通过CP和Tucker分解提取潜在协调结构，定义可计算的协调复杂度度量，并利用残差定位冲突。在2至4个智能体的多机器人送货任务中，2智能体100%收敛到无冲突计划，3智能体80%，4智能体60%，平均迭代次数随智能体增加上升。CP rank与智能体数近似线性增长，可作为规划难度预测指标。该方法无需领域规则即可解释冲突原因，适用于多智能体LLM迭代重规划。 \
**Motivation**：多智能体LLM独立规划导致空间碰撞、资源竞争等协调失败，现有方法缺乏通用冲突定位手段。 \
**Method**：将N智能体H步A动作联合计划建模为三阶张量，通过CP和Tucker分解提取潜在协调结构，定义协调复杂度并利用残差定位冲突。 \
**Result**：在2/3/4智能体任务中，无冲突计划收敛率100%/80%/60%，平均迭代1.4/3.2/4.0次，CP rank与智能体数线性相关。 \
**Conclusion**：Tensor-Coord通过代数分解为多智能体LLM规划提供可解释冲突分析，协调复杂度可预测规划难度。

---

## Abstract
Large language models (LLMs) remain limited in multi-agent planning because independently generated plans can create coordination failures such as spatial collisions, resource contention, and temporal deadlocks. We introduce Tensor-Coord, a multilinear algebra framework that represents the joint plan of N agents as a third-order tensor \(T \in R^{N \times H \times A}\) over agents, timesteps, and actions. Canonical Polyadic (CP) and Tucker decompositions are used to identify latent coordination structure. The minimal epsilon-approximate CP rank R* defines a computable coordination complexity measure, with \(CC(Pi)=(R*-N)/N\). We prove that R*=N is necessary and sufficient for plan independence. The residual \(E=T-T_{R*}\) defines a conflict score over agent pairs, timesteps, and actions, localizing failures without domain-specific rules. Tucker factors provide interpretable agent roles, temporal phases, and action clusters that are converted into natural language constraints for iterative LLM replanning. Experiments on multi-robot delivery tasks across Easy (2 agents, 5x5 grid), Medium (3 agents, 5x5 grid), and Hard (4 agents, 5x5 grid) settings show convergence to conflict-free plans in 100% of 2-agent cases within 1.4 iterations on average, 80% of 3-agent cases within 3.2 iterations, and 60% of 4-agent cases within 4.0 iterations. CP rank scaled approximately linearly as \(R*(N) = 3.9N + 0.5\), supporting its use as a predictor of coordination complexity.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：多智能体 LLM 规划中，各智能体独立生成计划常导致**空间碰撞、资源争用、时间死锁**等协调失败。LLM 本质上是概率生成模型，无法保证确定性的状态跟踪和约束满足，而多智能体场景下联合状态/动作空间指数增长，现有方法（联合提示、顺序规划、经典 Conflict‑Based Search）要么受限于上下文窗口，要么产生顺序偏差，要么需要形式化领域建模，难以兼顾 LLM 的自然语言灵活性与协调保障。
- **整体含义**：论文尝试用**多重线性代数**（张量分解）来代数化描述协调结构，将“计划是否冲突”转化为“张量秩是否等于智能体数”的数学问题，从而为多智能体 LLM 规划提供了一套**可计算、可解释、无领域规则**的冲突分析与迭代细化框架。

### 2. 论文提出的方法论
- **核心思想**：将 N 个智能体的联合计划 \( \Pi \in (A^T)^N \) 编码为一个**三阶软张量** \( \mathcal{T} \in \mathbb{R}^{N \times T \times K} \)（智能体×时间步×动作）。软编码采用置信度 \( \gamma=0.8 \) 分配概率，确保张量满秩、分解稳定。
- **关键技术细节**：
    - **CP 分解**：\( \mathcal{T} \approx \sum_{r=1}^R \lambda_r \, \mathbf{u}_r \otimes \mathbf{v}_r \otimes \mathbf{w}_r \)。最小 ε‑近似 CP 秩 \( R^* \) 定义为满足重构误差 ≤ ε 的最小 R。定义协调复杂度 \( CC(\Pi) = (R^*-N)/N \)。**定理 1** 证明 \( R^*=N \) 是计划独立性的充要条件。
    - **残差冲突检测**：残差张量 \( \mathbf{E} = \mathcal{T} - \mathcal{T}_{R^*} \)，每个智能体每步的残差范数 \( \rho(i,t)=\|\mathbf{E}(i,t,:)\|_2 \)，冲突分数 \( CS(i,j,t) = \rho(i,t) \cdot \rho(j,t) \)，阈值 τ=0.15 判定冲突。**命题 2** 证明碰撞必然导致 CS > τ。
    - **Tucker 分解**：\( \mathcal{T} \approx \mathcal{G} \times_1 \mathbf{U} \times_2 \mathbf{V} \times_3 \mathbf{W} \)，因子矩阵分别对应**智能体角色**、**时间阶段**、**动作类型**，生成三类自然语言约束：空间避让、阶段同步、角色错峰。
- **算法流程**（迭代细化）：
    1. 各智能体用 LLM 初步生成计划（基于初始约束集）。
    2. 将联合计划编码为软张量 \( \mathcal{T} \)。
    3. 用 ALS 求解 CP 分解得到 \( R^* \) 及近似张量。
    4. 计算残差并输出所有冲突事件 \( CS(i,j,t)>τ \)。
    5. 用 HOSVD 进行 Tucker 分解提取角色/阶段/动作类型。
    6. 根据冲突和 Tucker 因子生成自然语言约束，追加到各智能体提示中。
    7. 重复步骤 1‑6 直到无冲突或达到最大迭代次数 K。

### 3. 实验设计
- **场景与数据集**：多机器人配送任务，网格世界（5×5 或 6×6），动作空间 K=7（上下左右、取、放、等待）。共 **5 种配置**：
    - **Easy**：2 智能体，5×5 网格，2 包裹，T=10
    - **Medium**：3 智能体，5×5 网格，3 包裹，T=15
    - **Hard**：4 智能体，5×5 网格，4 包裹，T=20
    - **Very Hard**：5 智能体，6×6 网格，5 包裹，T=30
    - **Complex**：3 智能体，6×6 网格，6 包裹，T=35
- **Benchmark 与对比方法**：
    - 基线 1（独立规划）：各智能体独立生成计划，无协调。
    - 基线 2（轮询规划）：按顺序规划，后规划者能看到之前计划。
    - 基线 3（单 LLM 联合）：一个提示同时生成所有智能体计划。
    - LLM 后端：Azure OpenAI GPT‑5，温度 0.2，最大 token 500。
- **实验设置**：每个配置使用 **5 个随机种子**，最大迭代次数 **5** 次。表 1 报告了平均 CP Rank、剩余冲突数、CC 值、成功率。

### 4. 资源与算力
- 论文**未明确说明**使用的 GPU 型号、数量或训练时长。因为是 LLM 推理，仅通过 Azure OpenAI API 调用 LLM（GPT‑5），不涉及内部模型训练。张量分解（ALS、HOSVD）在 CPU 上即可完成，计算量远小于 LLM 推理。论文未提供具体计算资源信息。

### 5. 实验数量与充分性
- **实验数量**：5 种配置 × 5 种子 = 25 组运行（每个种子一次完整迭代流程）。但表 1 只给出平均结果，未报告方差或标准差；图 4 展示了与基线的对比；附录可能缺少细粒度过程数据。
- **充分性评价**：
    - 覆盖了 2‑5 智能体、不同网格尺寸和包裹数，问题难度递增，具有一定代表性。
    - 对比了三种常见 LLM‑based 基线，但没有与经典最优 MAPF 算法（如 CBS、M*）对比，作者解释为后者需要形式化领域模型，与 LLM 范式不兼容。
    - **缺乏消融实验**：未分析各组件贡献（如去掉 Tucker 约束生成、不同 γ 或 τ 的影响、不同 CP 阈值的作用）。
    - **迭代次数固定为 5**，未分析更少或更多迭代下的行为；成功率在 Very Hard 仅 20%，说明框架局限性未被充分探讨。
    - 整体公平，但**统计显著性缺失**，且实验规模较小，结论的泛化能力有限。

### 6. 论文的主要结论与发现
- **主结论**：Tensor‑Coord 在 2‑4 智能体任务中能有效减少冲突：Easy 100% 无冲突收敛（平均 1.4 次迭代），Medium 80%（3.2 次），Hard 60%（4.0 次）。Very Hard（5 智能体）仅 20% 收敛，表明框架在密集场景下仍有瓶颈。
- **CP rank 与智能体数线性相关**：\( R^*(N) \approx 3.9N+0.5 \)，\( R^2=0.94 \)，可作为协调复杂度预测指标。
- **残差冲突检测有效**：空间碰撞时 CS(i,j,t) 显著高于阈值。
- **LLM Fallback Paradox**：有时 LLM 因约束过多返回空响应（仅 wait），反而偶然降低残差并加速收敛，提示显式等待策略值得探索。
- **对比基线**：Tensor‑Coord 在所有配置上冲突减少率最高（100%→27%），优于独立规划、轮询、单 LLM 联合。

### 7. 优点
- **理论创新**：将计划独立性与张量秩等价，提供首项基于代数的协调复杂度度量。
- **无领域规则**：冲突检测仅依赖张量残差，无需人工定义碰撞检测规则。
- **可解释性**：Tucker 分解输出角色、阶段、动作类型，约束为自然语言，便于人机交互。
- **迭代细化**：闭环反馈，LLM 只需按约束重规划，无需修改模型。
- **实验设计系统**：逐步增加智能体数，难度递进，基线选择合理。

### 8. 不足与局限
- **成功率随智能体增多急剧下降**：5 智能体仅 20% 收敛，说明网格拥堵、约束交互导致非单调冲突演化，框架尚未解决密集场景。
- **缺乏与经典最优 MAPF 算法对比**：虽然作者认为不兼容，但可对比冲突率或求解时间，否则难以评估“逼近最优”的程度。
- **消融实验缺失**：未分析 CP 秩阈值 ε、软编码 γ、冲突阈值 τ 的敏感性，也未分离 Tucker 约束生成带来的增益。
- **实验统计不充分**：仅 5 个种子，未报告方差/置信区间；迭代次数上限固定为 5，未探索更多迭代是否会提升 Very Hard 成功率。
- **LLM 依赖**：LLM 可能返回无效/空计划，影响实验的可重复性和鲁棒性；仅使用单一 LLM（GPT‑5），未测试其他模型。
- **应用限制**：仅评估网格世界配送任务，未涉及更复杂的连续空间、部分可观察环境或异构智能体；网格最大 6×6，伸缩性未知。
- **计算开销**：未讨论 ALS 与 HOSVD 在更大 N、T、K 下的时间复杂度，且每次迭代需多次 LLM 调用，成本较高。

（完）
